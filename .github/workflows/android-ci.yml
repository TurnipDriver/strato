name: Android CI (Release APK)

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 60

    steps:
      - name: Checkout (recursive submodules)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0              # full history (safer for submodules pinned by commit)
          submodules: recursive       # <-- key fix
          lfs: false

      - name: Ensure submodules are initialized
        run: |
          git submodule sync --recursive
          git submodule update --init --recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept all Android SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Install NDK + CMake (match project)
        run: |
          sdkmanager --install "ndk;26.1.10909125" "cmake;3.22.1"
          echo "ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/26.1.10909125" >> $GITHUB_ENV

      - name: Grant execute permission for Gradle
        run: chmod +x gradlew

      - name: Clean caches (fresh CMake/Gradle)
        run: |
          rm -rf app/.cxx || true
          rm -rf ~/.gradle/caches || true

      - name: Build Release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: strato-release-apk
          path: app/build/outputs/apk/release/*.apk
